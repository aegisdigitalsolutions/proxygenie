# proxygenie

## Required repository secrets

Add the following GitHub Actions repository secrets:

- `UPSTREAM_HOST`
- `UPSTREAM_PORT`
- `UPSTREAM_USER`
- `UPSTREAM_PASS`

Set their values in repository **Settings → Secrets and variables → Actions**.